from bot import app
import commands
import inline_queries
import control
# 'commands' module defines all user commands.
# 'inline_queries' defines, obviously, all inline queries.
# 'control'  defines all control (admin) commands.
# They both add directly on the dispatcher, so importing is enough
